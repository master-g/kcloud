# 阶段 3：内存管理 (Memory Management)

## 阶段目标

三层上下文压缩

## 主要内容

### 3.1 第一层压缩：工具结果压缩

**触发时机**：每次调用 LLM 之前

**实现方式**：
- 将旧的 tool result 替换为占位符
- 保留工具调用的历史记录
- 压缩实际结果内容

**示例**：
```
原始：
Tool: read_file("src/main.rs")
Result: [1000行代码...]

压缩后：
Tool: read_file("src/main.rs")
Result: <placeholder: read_file src/main.rs, 1000 lines>
```

### 3.2 第二层压缩：阈值触发压缩

**触发时机**：达到上下文限制时

**实现方式**：
1. 动态计算阈值（当前模型最大上下文的一定比例，如 80%）
2. 超过阈值时：
   - 保存完整对话到磁盘
   - 让 LLM 生成摘要
   - 用摘要替换完整对话历史

**磁盘存储格式**：
```
.conversation/
├── 2024-01-01_143022.conversation  # 完整对话
├── 2024-01-01_143022.summary      # 摘要
└── ...
```

### 3.3 第三层压缩：用户主动压缩

**触发时机**：用户使用 `/compact` 命令

**实现方式**：
- 与第二层相同的方式
- 由用户主动触发
- 压缩程度最高

### 3.4 混合存储管理

**策略**：
- 活跃上下文保存在内存中（快速访问）
- 历史上下文缓存到磁盘（节省内存）
- 支持按需加载历史上下文

**数据结构**：
```rust
struct ContextManager {
    active_context: Vec<Message>,      // 内存中的活跃上下文
    max_tokens: usize,                   // 模型最大 token 数
    threshold_ratio: f64,               // 触发压缩的比例 (0.8)
    history_dir: PathBuf,              // 历史记录目录
}

enum Message {
    User(String),
    Assistant(String, Vec<ToolCall>),
    ToolResult { call_id: String, result: String },
    Compressed { summary: String, original_path: PathBuf },
}
```

## 实施步骤

### 3.1 第一层压缩实现

**步骤 3.1.1**: 设计占位符格式

**步骤 3.1.2**: 实现工具结果替换逻辑

**步骤 3.1.3**: 集成到 LLM 调用前处理

### 3.2 第二层压缩实现

**步骤 3.2.1**: 实现 token 计数

**步骤 3.2.2**: 实现阈值检测

**步骤 3.2.3**: 实现对话保存到磁盘

**步骤 3.2.4**: 实现 LLM 摘要生成

**步骤 3.2.5**: 实现历史替换逻辑

### 3.3 第三层压缩实现

**步骤 3.3.1**: 实现 `/compact` 命令

**步骤 3.3.2**: 复用第二层的压缩逻辑

### 3.4 混合存储实现

**步骤 3.4.1**: 设计上下文管理结构

**步骤 3.4.2**: 实现内存管理

**步骤 3.4.3**: 实现磁盘缓存

**步骤 3.4.4**: 实现按需加载

## 验收标准

- [ ] 第一层压缩正常工作（工具结果被正确替换为占位符）
- [ ] 第二层压缩正常工作（阈值触发时自动压缩）
- [ ] 第三层压缩正常工作（/compact 命令可以触发压缩）
- [ ] 对话历史正确保存到磁盘
- [ ] 摘要质量合理
- [ ] 混合存储正常工作
- [ ] 单元测试通过

## 技术细节

### 压缩提示词示例

```
请将以下对话压缩成简洁的摘要，保留关键信息：
- 用户的主要目标
- 已完成的重要操作
- 当前状态
- 待解决的问题

原始对话：
{conversation}
```

### 磁盘存储格式 (JSON)

```json
{
  "id": "2024-01-01_143022",
  "created_at": "2024-01-01T14:30:22Z",
  "messages": [...],
  "summary": "用户想要实现...",
  "token_count": 5000
}
```
